import { link } from "fs";
import { IoDocument } from "react-icons/io5";

const apps = [
  {
    icon: IoDocument,
    iconName: "Resume",
    fillColor: "white",
    backgroundColor: " bg-black",
    link: "/resume",
  },
  // Add more apps as needed
];

export default apps;
